interface:
  display_name: Test UI
  short_description: Run console UI tests from a Markdown plan
  default_prompt: Run the console UI test cases in test/ui-test-plan.md and stop at the first failure.
